name: CI

on:
  push:
    branches: [master, dev]
  # pull_request:
  #   branches: [master]

jobs:
  validate:
    name: Validate configs
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python (for pre-commit)
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Cache pre-commit environments
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: pre-commit-

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1

      - name: Prepare .env for CI
        run: |
          cat > .env << 'EOF'
          GF_ADMIN_USER=admin
          GF_ADMIN_PASSWORD=ci-dummy-password
          MSTEAMS_WEBHOOK_URL=https://dummy.webhook.url/ci
          TELEGRAM_BOT_TOKEN=0000000000:ci-dummy-token
          TELEGRAM_CHAT_ID=000000000
          PROMETHEUS_RETENTION=15d
          PROMETHEUS_SCRAPE_INTERVAL=15s
          PROMETHEUS_EVALUATION_INTERVAL=15s
          ALERT_CPU_THRESHOLD=90
          ALERT_MEMORY_THRESHOLD=90
          ALERT_DISK_THRESHOLD=85
          SLACK_WEBHOOK_URL=https://hooks.slack.com/ci-dummy
          SLACK_CHANNEL=#alerts
          SMTP_HOST=smtp.example.com:587
          SMTP_FROM=alert@example.com
          SMTP_USER=alert@example.com
          SMTP_PASSWORD=ci-dummy-smtp
          ALERT_EMAIL_TO=ops@example.com
          LINE_WEBHOOK_URL=https://dummy.line-proxy.example.com/notify
          EOF

      - name: Expand env vars in configs
        run: |
          export $(grep -v "^#" .env | xargs)
          envsubst < prometheus/prometheus.yml > /tmp/prometheus.yml
          envsubst < alertmanager/alertmanager.yml > /tmp/alertmanager.yml

      - name: Validate Prometheus config
        run: |
          docker run --rm \
            -v "/tmp/prometheus.yml:/tmp/prometheus.yml" \
            --entrypoint=promtool \
            prom/prometheus:v3.11.1 \
            check config /tmp/prometheus.yml

      - name: Validate Alertmanager config
        run: |
          docker run --rm \
            -v "/tmp/alertmanager.yml:/tmp/alertmanager.yml" \
            --entrypoint=amtool \
            prom/alertmanager:v0.32.0 \
            check-config /tmp/alertmanager.yml

      - name: Lint YAML files
        uses: ibiqlik/action-yamllint@v3
        with:
          config_data: |
            extends: relaxed
            rules:
              line-length:
                max: 120
